#include <stdio.h>

int main()
{

    // int i = 0;
    // while (i < 5)
    // {
    //     printf("%d\n", i);
    //     i++;
    // }

    // int age;
    // int year = 2023;
    // printf("Enter you age: ");
    // scanf("%d", &age);

    // while (age > 0 ){
    //     printf("You was %d years ole in %d. \n", age, year);
    //     age --;
    //     year --;
    // }

    /// For calculating countdown 1

    // int countdown = 5;

    // printf("Countdown from %d:\n", countdown);
    // while (countdown > 0) {
    //     printf("%d\n", countdown);
    //     countdown--;
    // }

    // printf("Blastoff!\n");

    /// For calculating numbers between 1 and 10       2

    // int number;

    // printf("Enter a number between 1 and 10: ");
    // scanf("%d", &number);

    // while (number < 1 || number > 10) {
    //     printf("Invalid input! Enter a number between 1 and 10: ");
    //     scanf("%d", &number);
    // }

    // printf("You entered: %d\n", number);

    //// Multiplication table for a given number 3

    // int num = 7;
    // int multiplier = 1;

    // printf("Multiplication table for %d:\n", num);
    // while (multiplier <= 10)
    // {
    //     printf("%d x %d = %d\n", num, multiplier, num * multiplier);
    //     multiplier++;
    // }

    return 0;
}
